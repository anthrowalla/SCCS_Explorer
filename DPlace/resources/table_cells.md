When a table cell is clicked it opens a modal window with information about the societies in the cell. The source of the       
  information about societies should now come from owc_info.json. Probably best to put owc_info.json into the resources folder    
  first. Then rewrite the code for display each society to use the info from the data field, which has 186 entries each           
  corresponding to the societies in order of their SCCS id.       
